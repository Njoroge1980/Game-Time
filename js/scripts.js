$(document) .ready (function() {
  $("#pong").submit(function(event) {
      var  input= ("input#")
      var increase= [];
         for( var number=1; number<=(input); number+=1) {
               alert(number);
             }
           event.preventDefault();
           console.log(input);

              });
          });
